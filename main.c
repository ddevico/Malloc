# include "libft/includes/libft.h"
# include "libft/includes/header.h"
# include "libft/includes/get_next_line.h"
    int main(void)
{
    size_t *page = NULL;
    page = (size_t *)malloc(sizeof(size_t));
    ft_printf("SMALL: 0x%lX\n", (unsigned long)page);
    
    return (0);
}